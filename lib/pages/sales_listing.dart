import 'package:flutter/material.dart';

class SalesListing extends StatelessWidget {
  const SalesListing({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Sales Listing"),
    );
  }
}
