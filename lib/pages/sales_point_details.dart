import 'package:flutter/material.dart';

class SalesPointDetails extends StatelessWidget {
  const SalesPointDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Sales Point Details"),
    );
  }
}
